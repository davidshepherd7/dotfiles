#name : for loop over boundary nodes
# --
for(unsigned b=0, nb=${1:mesh_pt()}->nboundary(); b<nb; b++)
{
for(unsigned nd=0, nnd=$1->nboundary_node(b); nd<nnd; nd++)
{
Node* nd_pt = $1->boundary_node_pt(b,nd);
$0
}
}