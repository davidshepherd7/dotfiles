#!/bin/bash -eux

set -o pipefail

BRANCH="$1"
COMMIT="$(git rev-parse HEAD)"

git stash

git checkout dev -b "david/$BRANCH"
git cherry-pick "$COMMIT"
wave git make-pr

# Go back
git switch -
git stash pop
