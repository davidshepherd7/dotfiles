# -*- mode: snippet; require-final-newline: nil -*-
# key: startcatch
# --
#include "${1:`(replace-regexp-in-string "tests/" "" (cpp-to-h-path (file-relative-name (buffer-file-name) (projectile-project-root))))`}"

#include "catch/catch.hpp"

TEST_CASE("${2:`(file-name-base (buffer-file-name))`}")
{
$0
}