# -*- mode: snippet; require-final-newline: nil -*-
# name: start
# key: start
# --
#!/bin/bash

set -o errexit
set -o nounset

SCRIPTDIR="\$( cd "\$(dirname "\$(readlink -f "\${BASH_SOURCE[0]}")")" && pwd )"
cd "$SCRIPTDIR/../"
