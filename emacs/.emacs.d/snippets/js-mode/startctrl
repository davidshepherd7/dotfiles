# -*- mode: snippet; require-final-newline: nil -*-
# key: startctrl
# --
(function() {
    'use strict';

angular.module('boron.$1.${2:`(s-dashed-words (ds/buffer-basename))`}', [
])
${2:.controller('${3:`(s-upper-camel-case (ds/buffer-basename))`}Ctrl', function ($scope) {
$0
})}
;

})();
